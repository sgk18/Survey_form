// Performance: Use strict mode and efficient event listeners
'use strict';

// DOM Elements
const body = document.body;
const themeToggle = document.getElementById('theme-toggle');
const showIngredients = document.getElementById('show-ingredients');
const showInstructions = document.getElementById('show-instructions');
const ingredientsContent = document.getElementById('